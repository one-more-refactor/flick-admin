{
  "name": "flick-admin",
  "version": "1.1.0",
  "description": "The flick server admin panel — the corepanel toolkit (now in-tree at src/lib) + the flick admin API.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "preview": "vite preview"
  },
  "dependencies": {
    "svelte": "^5.56.8"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7",
    "svelte-check": "^4.7.4",
    "typescript": "^5.8.0",
    "vite": "^8"
  }
}
